# Copy this file to .env and fill in your values.
# Never commit .env — it is excluded by .gitignore.

# Google Gemini API key (required by all agents)
# Get one at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Optional: use Vertex AI instead of Gemini Developer API
# GOOGLE_GENAI_USE_VERTEXAI=TRUE
# GOOGLE_CLOUD_PROJECT=your-gcp-project
# GOOGLE_CLOUD_LOCATION=us-central1

# ── Azure OpenAI (Kaitlynn Agent) ────────────────────────────────────────────
# Required only for the Kaitlynn (LangGraph) agent.
# All other agents use GOOGLE_API_KEY.
# Get these from your Azure OpenAI resource in the Azure Portal.
AZURE_OPENAI_API_KEY=your-azure-openai-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
# See: https://learn.microsoft.com/azure/ai-services/openai/api-version-deprecation
AZURE_OPENAI_API_VERSION=2024-08-01-preview

# ── MoQ Lite Relay (optional) ────────────────────────────────────────────────
# When set, all agents connect to the relay instead of listening on individual
# ports. The host agent will auto-discover friends via SUBSCRIBE_NAMESPACE.
# Leave commented out to use classic direct-connect mode (no relay needed).
# MOQT_RELAY_URL=moqt://localhost:20000
